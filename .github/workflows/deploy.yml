on:
  push:
    branches:
      - main
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    # - name: 🔨 Build Project
    #   run: |
    #     npm install
    #     npm run build  

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.3
      with:
        server: vs2389.mirohost.net
        username: testvb
        password: ${{ secrets.password }}
        server-dir: ./test.com.ua/
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          fileToExclude.txt